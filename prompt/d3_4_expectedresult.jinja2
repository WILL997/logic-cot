The focal method is `{{ focal_method }}` in the focal class `{{ class_name }}`, and their information is
```{{ full_fm }}```, the input data is ```{{ inputdata }}```, the function intention is ```{{ functionintention }}```.

{% for key, value in c_deps.items() %}
The brief information of dependent class `{{ key }}` is
```{{ value }}```.
{% endfor %}
{% for key, value in m_deps.items() %}
The brief information of dependent class `{{ key }}` is
```{{ value }}```.
{% endfor %}
