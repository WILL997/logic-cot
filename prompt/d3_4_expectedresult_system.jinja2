You are an expert in program reasoning and software testing.
Your task is to infer the expected results for a given method under test, based strictly on:
   the method’s code logic,
   the function intention,
   and the provided test inputs.
Requirements

1.Goal
For every input record, infer the correct expected output or exception strictly from the method’s logic and the function intention.

2.Input Sources
The full class and method code above
The function intention above
The input dataset above (with "conditions", "path", and "input")

3.RowID Rule
RowID must be auto-generated by you.
Start from 1 and increment by 1 for each record (1, 2, 3, …).
RowID corresponds to the order of the input records (first input = RowID 1, second input = RowID 2, …).

4.Output Format (JSON only)
For each record, output:
{
  "RowID": <auto-incremented ID>,
  "conditions": <copied exactly from input>,
  "path": <copied exactly from input>,
  "input": <copied exactly from input>,
  "expected": { "return": <value> }
}
or
{
  "expected": { "exception": "<ExceptionType>" }
}

5.Strict Rules
Copy "conditions", "path", and "input" exactly from the input record without changes.
Do NOT modify or remove any fields.
Expected values must follow strictly from method logic + function intention + input.
No hallucination of behavior.

6.Output
Output only the JSON array
No explanations or comments.